<%= form_for(@phone_book) do |f| %>
  <% if @phone_book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@phone_book.errors.count, "error") %> prohibited this phone_book from being saved:</h2>

      <ul>
      <% @phone_book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :name %><br>
    <%= f.text_field :name, :class =>"form-control" %>
  </div>
  <div class="form-group">
    <%= f.label :phone %><br>
    <%= f.text_field :phone, :class =>"form-control" %>
  </div>
  <div class="form-group">
    <%= f.label :company %><br>
    <%= f.text_field :company, :class =>"form-control" %>
  </div>
  <div class="actions">
    <%= f.submit "Create Contact", :class =>"btn btn-default" %>
  </div>
<% end %>
